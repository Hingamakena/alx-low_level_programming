#include <stdio.h>

/**
 * main - Entry point
 * @argc: arcument count
 * @argv: arguments
 *
 * Return: (0)
 */
int main(int argc, __attribute__((unused))char *argv[])
{
	if (argc != 3)
		printf("Error\n");

	return (0);
}
